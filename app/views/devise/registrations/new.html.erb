<div class="columns">
  <div class="column is-one-quarter is-offset-4">
    <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
    <div class="card">
      <%= error_messages_for(resource) %>
      <div class="card-content">
	<div class="content">

	  <div class="field">
	    <%= f.label :username, class: "label" %>
	    <div class="control">
	      <%= f.text_field :username, autofocus: true, class: "input", required: true %>
	     </div>
	  </div>

	  <div class="field">
	    <%= f.label :password, class: "label" %>
	    <div class="control">
	      <%= f.password_field :password, autocomplete: "off", class: "input", required: true %>
	    </div>
	  </div>

	  <div class="field">
	    <%= f.label :password_confirmation, class: "label" %>
	    <div class="control">
	      <%= f.password_field :password_confirmation, autocomplete: "off", class: "input", required: true %>
	    </div>
	  </div>
	</div>
      </div>

      <footer class="card-footer">
	<p class="card-footer-item">
	  <%= link_to "Sign in", new_session_path(resource_name), class: "button is-link" %>
	</p>
	<p class="card-footer-item">
	  <%= f.submit "Sign up", class: "button is-primary" %>
	</p>
      </footer>
    </div>
    <% end %>
  </div>
</div>
